package com.lrj.workflow.sdk;

/**
 * SDK 占位。Phase 3 落地 WorkflowClient / NoopWorkflowClient / WorkflowSdkAutoConfiguration
 * (enabled 默认 false)。当前仅确保模块可独立编译。
 */
public final class SdkInfo {

    private SdkInfo() {
    }
}
